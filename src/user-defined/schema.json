{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsUserDefined",
  "title": "User Defined files Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the files.",
      "type": "string"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the files.",
      "visible": false
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "env": {
      "type": "string",
      "description": "environment"
    }
  },
  "required": ["name"]
}
