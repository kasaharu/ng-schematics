{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsRepository",
  "title": "Repository Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the repository.",
      "type": "string"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the repository.",
      "visible": false
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "env": {
      "type": "string",
      "description": "environment"
    }
  },
  "required": ["name"]
}
